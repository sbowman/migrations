// Package tests is set up to run as a standalone solution, so the PostgreSQL database library
// doesn't get imported with your project when you use the migrations package, but can still be
// used to test the database migrations with at least one database.
package tests
